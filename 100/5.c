/* 题目：输入三个整数x,y,z，请把这三个数由小到大输出。 */

#include <stdio.h>

int main()
{
	int x, y, z;
	
	printf("Enter your numbers(x, y, z): ");
	scanf("%d, %d, %d", &x, &y, &z);
	
	printf("Sort by the value: \n");
	
	
}
